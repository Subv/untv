ul.menu-list
  for item in items
    - var type = (item.handler) ? "action" : "extension"
    - var id   = (type === "action") ? item.id : ""
    li.animated(data-type=type, data-action-id=id)
      .menu-item-wrapper  
        if (item === items[0])
          span.inactive.up.scroll-indicator &laquo;
        else
          span.active.up.scroll-indicator &laquo;
        .menu-item
          div
            img(src="#{item.icon}", width="84", height="84")
            strong.menu-name #{item.name}
            p.menu-description #{item.description}
        if (item === items[items.length -1])
          span.inactive.down.scroll-indicator &raquo;
        else
          span.active.down.scroll-indicator &raquo;
header#status-bar
  ul
    li.clock 12:00 AM
    li.remote-connection
      span.icon Remote: 
      span.status Connecting...
    li.internet-connection
      span.icon Internet:
      span.status Checking...
