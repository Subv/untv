ul.file-selector
  for file in files
    if file.stats.isDirectory()
      li(data-path="#{file.path}", data-type="directory") #{file.name}
    else
      li(data-path="#{file.path}", data-type="file") #{file.name}
